# Plans

This section is a local workspace for planning notes while developing `django-powercrud`.

Keep active plans as untracked files under `docs/mkdocs/plans/` (they are ignored by git), and promote anything worth keeping into the formal `guides/` or `reference/` docs when finished.
